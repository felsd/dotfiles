# applications
super + c
  chromium
super + p
  chromium --incognito
super + g
  galculator
super + r
  urxvtc -e /bin/bash -c "xrdb ~/.Xresources && ranger ~/" 
super + k
  keepassxc
super + t
  thunderbird
super + v
  virtualbox
